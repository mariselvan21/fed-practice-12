const outputDevices=["monitor","printer"];
console.log("original array of outputDevices:"+outputDevices);
var computerDevices=outputDevices.valueOf();
console.log("valueOf method using outputDevices array:"+computerDevices);
console.log("this valueOf method produce same input value")
